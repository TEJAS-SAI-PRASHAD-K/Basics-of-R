#vectors
fruits <- c("banana", "apple", "orange")

# Print fruits
fruits

numbers <- 1:10

numbers

#length of vector

fruits <- c("banana", "apple", "orange")

length(fruits)

#sort a vector

fruits <- c("banana", "apple", "orange", "mango", "lemon")
numbers <- c(13, 3, 5, 7, 20, 2)

sort(fruits)
sort(numbers)

#repeat a vector

repeat_each <- rep(c(1,2,3), each = 3)

repeat_each

#repeat n times a vector

repeat_times<- rep(c(1,2,3), times = 3)

repeat_times

#repeat each independent values

repeat_indepent <- rep(c(1,2,3), times = c(5,2,1))

repeat_indepent

#sequence of vector
numbers <- seq(from = 0, to = 100, by = 20)

numbers

#list
thislist <- list("apple", "banana", "cherry")

# Print the list
thislist

#accessing the list

thislist <- list("apple", "banana", "cherry")

thislist[1]


